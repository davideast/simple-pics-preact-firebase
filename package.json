{
  "name": "simple-pics-base",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build:clean": "rm -rf dist",
    "build:client": "yarn build:clean && webpack",
    "build:prod": "yarn build:clean && webpack -p"
  },
  "dependencies": {
    "firebase": "^4.10.1",
    "firebase-tools": "^3.17.6",
    "preact": "^8.2.7",
    "preact-async-route": "^2.1.1",
    "preact-router": "^2.6.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^4.5.1",
    "css-loader": "^0.28.10",
    "mini-css-extract-plugin": "^0.2.0",
    "ts-loader": "^4.0.1",
    "typescript": "^2.7.2",
    "webpack": "^4.1.1",
    "webpack-cli": "^2.0.10",
    "workbox-cli": "^3.0.0"
  }
}
